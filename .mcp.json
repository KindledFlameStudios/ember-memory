{
  "mcpServers": {
    "ember-memory": {
      "command": "python3",
      "args": [
        "-c",
        "import sys; sys.path.insert(0, '${CLAUDE_PLUGIN_ROOT}'); from ember_memory.server import mcp; mcp.run(transport='stdio')"
      ],
      "env": {
        "EMBER_AI_ID": "claude"
      }
    }
  }
}
